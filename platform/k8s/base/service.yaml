apiVersion: v1
kind: Service
metadata:
  name: mcp-briefing-agent
  labels:
    app.kubernetes.io/name: mcp-briefing-agent
    app.kubernetes.io/component: service
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mcp-briefing-agent
    app.kubernetes.io/component: service
  ports:
    - name: http
      port: 8787
      targetPort: http
